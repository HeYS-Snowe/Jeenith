# 修复扫描发现的全部 16 项审计问题

**类型**：fix
**日期**：2026-07-12 22:50
**影响范围**：mobile/lib（16 个文件）+ mobile/android/app/build.gradle.kts

## 变更描述

按 bug-detector 审计报告，修复 2 个 High、6 个 Medium、8 个 Low 级问题。
Info 4 项为正面确认（无需修复）。修复后 `flutter analyze` 0 issues。

## 修改详情

### High（2 项）

- **H1 release 签名安全** — [build.gradle.kts](file:///d:/Code/Project/Qore/Jeenith/mobile/android/app/build.gradle.kts)
  - 原：release 直接复用 debug 签名，公开密钥，无法上架
  - 改：读取 `key.properties`（git-ignored）配置 release keystore；缺省回退 debug key 仅适用 dev 调试

- **H2 梅花易数随机起卦绕过真随机** — [meihua_page.dart](file:///d:/Code/Project/Qore/Jeenith/mobile/lib/features/meihua/ui/meihua_page.dart)
  - 原：`_onRandom()` 用 `DateTime.now().microsecond % 100` 时间戳模运算绕过 TrueRandom 引擎
  - 改：调用 `divineRandom()` 走项目 3 源 SHA256 真随机

### Medium（6 项）

- **M1 HistoryStore 并发竞态** — [history_store.dart](file:///d:/Code/Project/Qore/Jeenith/mobile/lib/core/history/history_store.dart)
  - 原：add/updateNote/remove 均为非原子 load→modify→save，并发调用会互相覆盖
  - 改：加 `_serialize<T>()` 串行化所有写操作（chain Future）；新增 `generateId()` 单调递增 ID（避免 microsecondsSinceEpoch 同帧碰撞）；新增 `clear()` 方法

- **M2 ConfigNotifier 加载期崩溃** — [config_providers.dart](file:///d:/Code/Project/Qore/Jeenith/mobile/lib/core/config/config_providers.dart)
  - 原：`state.valueOrNull!.copyWith(...)` 在初次加载未完成时抛 NPE
  - 改：`await future` 等待 build 完成后再写 state

- **M3 小六壬输入解析崩溃 + 动画越界 + 头部裁切** — [xiaoliuren_page.dart](file:///d:/Code/Project/Qore/Jeenith/mobile/lib/features/xiaoliuren/ui/xiaoliuren_page.dart)
  - 原：`int.parse(s)` 对超长数字串抛 FormatException；`Interval(0.28+i*0.1, 0.52+i*0.1)` 卡片 ≥8 张时上界超 1.0；pinned header 170 高度裁切内容
  - 改：`int.tryParse(s) ?? 6`；`Interval(...).clamp(0.0, 1.0)`；header 高度 170 → 200

- **M4 Starfield 全局 60fps 持续渲染** — [starfield.dart](file:///d:/Code/Project/Qore/Jeenith/mobile/lib/shared/widgets/starfield.dart)
  - 原：`AnimationController..repeat()` 14s 周期，置于 MaterialApp.builder 中所有页面常驻；后台不暂停
  - 改：duration 14s → 30s 降低视觉运动；加 `WidgetsBindingObserver` 监听 `AppLifecycleState`，非 resumed 态自动 stop，resumed 时 repeat

- **M5 TextPainter 未 dispose（3 处）** — [divination_wheel.dart](file:///d:/Code/Project/Qore/Jeenith/mobile/lib/features/xiaoliuren/ui/divination_wheel.dart) / [hexagram_view.dart](file:///d:/Code/Project/Qore/Jeenith/mobile/lib/features/zhouyi/ui/hexagram_view.dart) / [xiaoliuren_ritual.dart](file:///d:/Code/Project/Qore/Jeenith/mobile/lib/features/xiaoliuren/ui/xiaoliuren_ritual.dart)
  - 原：`_drawText` 创建 TextPainter 后未 `dispose()`，每帧泄漏原生文本布局资源
  - 改：`tp.paint(...)` 后加 `tp.dispose()`

- **M6 紫微/奇门日期缺上界校验** — [ziwei_page.dart](file:///d:/Code/Project/Qore/Jeenith/mobile/lib/features/ziwei/ui/ziwei_page.dart) / [qimen_page.dart](file:///d:/Code/Project/Qore/Jeenith/mobile/lib/features/qimen/ui/qimen_page.dart)
  - 原：仅校验 `y<1900 || m<1 || d<1 || h<0 || h>23`，缺月份 >12、日期 >31、年份 >2100 上界
  - 改：补 `y>2100 || m>12 || d>31`

### Low（8 项）

- **L1 random.org 响应格式误提取** — [online_entropy_source.dart](file:///d:/Code/Project/Qore/Jeenith/mobile/lib/core/rng/online_entropy_source.dart)
  - 原：`RegExp(r'\d+').allMatches(res.body)` 从错误页 HTML 中也会提取数字
  - 改：用 `LineSplitter` 拆行 + 严格 `int.tryParse` + 1-9 范围校验，6 行齐全才算成功

- **L2 HistoryStore.add 未 await（6 处）** — 各 feature 页面
  - 原：`HistoryStore.add(...)` 不 await，Future 漂浮；异常被吞
  - 改：用 `unawaited(...)` 显式标注 fire-and-forget 意图（lint 友好，且不阻塞 UI）

- **L3 历史记录 ID 用 microsecondsSinceEpoch（6 处）** — 各 feature 页面
  - 原：`DateTime.now().microsecondsSinceEpoch.toString()` 同一帧多次调用会碰撞
  - 改：统一用 `HistoryStore.generateId()`（微秒 + 自增计数）

- **L4 死代码（3 处）** — [divination_tech.dart](file:///d:/Code/Project/Qore/Jeenith/mobile/lib/core/divination/divination_tech.dart) / [xiaoliuren_tech.dart](file:///d:/Code/Project/Qore/Jeenith/mobile/lib/features/xiaoliuren/xiaoliuren_tech.dart) / [platform_info.dart](file:///d:/Code/Project/Qore/Jeenith/mobile/lib/core/config/platform_info.dart)
  - 原：`DivinationTech.usesTrueRandom`（仅声明，0 消费方）、`PlatformInfo.hasTouch`（仅声明，0 消费方）
  - 改：删除（Grep 全 lib/ 确认无引用）

- **L5 divinationRegistry 无重复 ID 校验** — [divination_registry.dart](file:///d:/Code/Project/Qore/Jeenith/mobile/lib/core/divination/divination_registry.dart)
  - 原：列表中 ID 重复时 `techByIdProvider.firstOrNull` 静默命中错误项
  - 改：加 `assert(techs.map((t)=>t.id).toSet().length == techs.length, ...)`

- **L6 小六壬 `int.parse` 崩溃** — 与 M3 同源，已在 M3 修复
- **L7 小六壬 Interval 越界** — 与 M3 同源，已在 M3 修复

- **L8 历史记录无清空入口** — [history_page.dart](file:///d:/Code/Project/Qore/Jeenith/mobile/lib/features/history/history_page.dart)
  - 原：八字明文存储且无"一键清空"功能
  - 改：AppBar 加 delete_sweep 按钮，二次确认对话框调用 `HistoryStore.clear()`

### 附加修复（顺手清理）

- 修复项目历史遗留 info：[divination_wheel.dart:44](file:///d:/Code/Project/Qore/Jeenith/mobile/lib/features/xiaoliuren/ui/divination_wheel.dart#L44) `library_private_types_in_public_api` —— 将 public 字段 `trail` 重命名为私有 `_trail`（外部无消费方）

## 影响分析

- **签名安全**：release 包不再暴露 debug 密钥，可正式上架
- **真随机完整性**：梅花随机起卦回到 3 源真随机路径，卜算随机性符合设计
- **数据完整性**：历史记录并发写入不再丢条；ConfigNotifier 加载期不再崩溃
- **稳定性**：超长数字输入、卡片数量异常时不再崩溃
- **资源**：TextPainter 不再每帧泄漏；Starfield 后台不再耗电
- **隐私**：用户可一键清空全部卜算历史

## 验证

```bash
cd mobile
flutter analyze   # No issues found! (0 issues)
```

## 相关问题

审计报告：bug-detector agent 输出（High 2 / Medium 6 / Low 8 / Info 4）
