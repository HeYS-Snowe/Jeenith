# 文档版本同步至 v2.10.3 + 清理废弃引导组件

**类型**：docs / refactor
**日期**：2026-07-23 10:30
**影响范围**：AGENTS.md、CLAUDE.md、mobile/lib/shared/widgets/tech_guide_dialog.dart

## 变更描述

承接上一智能体「构建产物批量重命名」交接文档后的收尾工作。交接文档称工作区 clean，但实际核验发现工作区有 6 个文件被反向改回旧格式（脏改动），且 AGENTS.md / CLAUDE.md 的版本号严重过时（停留在 2.4.1+25，实际已 2.10.3+43），同时存在一个未被引用的孤儿组件 `tech_guide_dialog.dart`。

本次完成：
1. 丢弃工作区 6 个文件的反向脏改动，恢复新格式（与 commit `57631af` 一致）
2. 清理零引用的废弃组件 `tech_guide_dialog.dart`
3. 同步 AGENTS.md / CLAUDE.md 版本号、features 列表、release 历史

## 修改详情

- [AGENTS.md]：版本号 `2.4.1+25（fix，2026-07-18）` → `2.10.3+43（fix，2026-07-22）`；features 列表补入 `taiyi / liuyao / chenggu`（v2.6～v2.8 新增的第 13～15 术）
- [CLAUDE.md]：版本号同步至 `2.10.3+43`；第二节 features 列表补入 `taiyi / liuyao / chenggu`；第五节「后续阶段」补全 v2.4.x～v2.10.3 共 10 条 release 记录，并从「后续可考虑」移除已落地的「首次使用引导遮罩」
- [mobile/lib/shared/widgets/tech_guide_dialog.dart]：**删除**。该文件（7/20 创建，单页 5 点导读，零引用）是 `tech_guide_overlay.dart`（7/19 创建，已接入紫微/奇门/大六壬三术，多步 GuideStep + 3 秒倒计时禁关）的废弃替代版本，功能被后者覆盖且后者体验更优。删除后 `flutter analyze` 通过（No issues found）

## 影响分析

- **无运行时影响**：`tech_guide_dialog.dart` 零引用，删除不改变任何行为；三术首次引导仍由 `tech_guide_overlay.dart` 提供
- **文档准确性提升**：AGENTS.md / CLAUDE.md 现与 pubspec.yaml（2.10.3+43）、实际 features 目录（19 个）、git release 历史一致，避免误导后续智能体
- **工作区恢复 clean**：丢弃脏改动后仅剩本次新增的文档/日志改动，可独立提交

## 相关问题

承接交接文档：`.trae/documents/build-artifact-rename-handover.md` 第七节「untracked 文件处理」项（原建议确认是否纳入版本控制，经核验为废弃死代码，已删除）
